# AP2 fixtures

Placeholder. AP2 is still draft upstream (`google-agentic-commerce/AP2`), so `@xpaysh/ap2-schemas` ships `SPEC_VERSION = 'draft'`. Real mandate-verification fixtures land once Google publishes a stable spec tag.
